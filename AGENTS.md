# AGENTS.md

## Cursor Cloud specific instructions

This is a freshly initialized repository (`cat99sky`) with no application code, dependencies, or services yet. The repository contains only a `README.md` placeholder.

- **No build/lint/test commands** are available — there are no source files, package manifests, or test frameworks configured.
- **No services** need to be started for development.
- When code is added to this repository, update this section with relevant build, lint, test, and run instructions.
